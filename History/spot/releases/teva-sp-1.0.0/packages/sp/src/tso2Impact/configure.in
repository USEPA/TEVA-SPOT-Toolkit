# file is a shell script fragment that supplies the information
# necessary to tailor a template configure script into the configure
# script appropriate for this directory.  For more information, check
# any existing configure script.

srctrigger=Makefile.in
srcname="sp/src/tso2Impact"

# per-host:

host_makefile_frag=../../config/mf-sp-${with_host}-${with_target}${with_compiler}-${site}
site_makefile_frag=

# per-target:

Makefile=Makefile-${with_target}
touch $Makefile
files="Makefile-${with_target}"
links="Makefile"

